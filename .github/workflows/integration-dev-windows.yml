name: Integration Tests Windows Dev

on:
  # schedule:
  #   - cron: "30 0 * * *"
  workflow_dispatch:

jobs:
  integration-tests:
    uses: Genez-io/genezio-tests/.github/workflows/integration-tests.yml@fix/GNZ-795
    with:
      branch: "dev"
      os: windows-latest
    secrets:
      token: ${{ secrets.GENEZIO_TOKEN_DEV }}
