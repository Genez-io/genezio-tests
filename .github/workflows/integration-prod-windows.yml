name: Integration Tests Windows Production

on:
  # schedule:
  #   - cron: "30 0 * * *"
  workflow_dispatch:

jobs:
  integration-tests:
    uses: Genez-io/genezio-tests/.github/workflows/integration-tests.yml@fix/GNZ-795
    with:
      branch: "master"
      os: windows-latest
    secrets:
      genezio_token: ${{ secrets.GENEZIO_TOKEN_PROD }}
